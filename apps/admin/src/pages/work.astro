---
import AdminLayout from "../layouts/AdminLayout.astro";
import { WorkView } from "../components/astryx/UnifiedShellViews";

const isLoading = Astro.url.searchParams.get("state") === "loading";
---

<AdminLayout title="work" deck="Projects, tasks, and history.">
  <WorkView isLoading={isLoading} />
</AdminLayout>
