// Re-export from shared data package (single source of truth)
export { projects, liveProjects } from "@anipotts/lib/data";
export type { Project, ProjectStatus, ProjectCategory } from "@anipotts/types";
